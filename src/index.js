import React from 'react'; //main librabry
import ReactDOM from 'react-dom'; //has to do with rendering in the browser
import App from './App'; //main app compnent

ReactDOM.render(<App />, document.getElementById('root')); //getting element 'root' because that's where we want to render this application
